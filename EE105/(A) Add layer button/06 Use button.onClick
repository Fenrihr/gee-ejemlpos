ui.root.clear();

function addLabel() {
  var label = new ui.Label("Hello, world!");
  ui.root.add(label);
}

var button = new ui.Button({
  label: 'Click me!',
});

ui.root.add(button);

button.onClick(addLabel);